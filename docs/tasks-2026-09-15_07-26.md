# Tasks — Phase 1 Deal Simulation

**Session:** 2026-09-15_07-26  
**Plan:** [plan-2026-09-15_07-26.md](./plan-2026-09-15_07-26.md)  
**Status:** Phase 1 implemented  
**Rules:** [rules.md](./rules.md) (Fandom text, 2026-09-15)

Mark items `[x]` only after the work is done.

## 0. Approval gate

- [x] User accepted the Phase 1 plan (or a written amendment of it)

## 1. Scaffold

- [x] Initialize Vite + React + TypeScript at the repository root
- [x] Add Three.js, `@react-three/fiber`, `@react-three/drei`
- [x] Add `Makefile` with `install`, `dev`, `build`, `preview`, `clean`
- [x] Add `.gitignore` for `node_modules`, `dist`, editor junk
- [x] Add a short English `README.md` with `make dev` as the only setup step
- [x] Confirm `make dev` opens `http://127.0.0.1:5173`

## 2. Deal engine (`src/game/`)

- [x] Define `DealParams`, `Card`, `PlayerHand`, `Deal` types
- [x] Implement seeded PRNG (`mulberry32`)
- [x] Implement 52-card deck factory and Fisher–Yates shuffle
- [x] Implement `deal(params, seed): Deal`
  - [x] 7 regular cards per player
  - [x] 1 shotgun per player
  - [x] 1 zombie assigned to one random player
  - [x] `vaccineCount` vaccines on distinct random players
- [x] Validate `playerCount ∈ [2, 16]` and `vaccineCount ∈ [0, playerCount]`
- [x] Same seed + same params ⇒ identical `Deal`

## 3. Scene (`src/scene/`)

- [x] Sterile white-lab lighting (cool fluorescents, cyan signage, bright floor)
- [x] White rectangular table + center metallic dome (set dressing only)
- [x] `PlayerSeat` along the two long sides (2–16)
- [x] `CardMesh` with procedural faces (regular + 3 specials)
- [x] Staggered deal animation from deck to seats, then fan
- [x] Orbit / zoom controls (damped)
- [x] Special cards slightly raised and tinted for readability

## 4. UI (`src/ui/`)

- [x] Players input (2–16)
- [x] Vaccines input (clamped to current player count)
- [x] Optional seed field
- [x] Deal / Redeal actions
- [x] Legend: player count, zombie holder, vaccine holders, seed
- [x] Disable Deal while an animation is in flight (or queue one redeal)

## 5. Wiring and polish

- [x] `App` owns params + current `Deal` and passes them to scene + UI
- [x] Default deal on first load (16 players, 2 vaccines)
- [x] Overlay does not block orbit on the canvas
- [x] 16-player layout remains readable
- [x] Browser check of the deal flow (change params → Deal → inspect hands)

## 6. Close-out

- [x] Mark completed tasks in this file
- [x] Update [plan.md](./plan.md) if Phase 1 decisions changed
- [ ] Commit only if the user asks
