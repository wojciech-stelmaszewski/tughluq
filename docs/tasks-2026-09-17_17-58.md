# Tasks — RL stage B: linear ES trainer (2026-09-17_17-58)

Stage B only. Linear scorer, no MLP, no PPO.

## Scorer

- [x] 1. `src/rl/scorer.ts`: `score(weights, view, play)` = dot product of
      `encodeViewVector(view)` ⊕ `encodeAction(play, view)`.
- [x] 2. `scoredPolicy(weights): Policy` — softmax over legal scores, **sample** with
      `ctx.random`. Deterministic given weights + rng stream.
- [x] 3. Zero weights ⇒ uniform over legal actions (same as `randomLegal` in distribution).

## Trainer

- [x] 4. `src/rl/train.ts`: each generation, sample P perturbed copies of `w`, evaluate
      each with the Stage A differential harness (8 vs 56, N seeds), keep the elite.
- [x] 5. Reference starts as `randomLegal`. Every K generations, if elite fitness > 0
      vs the current reference, freeze elite weights as the new reference policy.
- [x] 6. Log per generation: elite fitness vs reference, elite vs `randomLegal`, elite vs
      `aggressive`, population zombie share when all 64 seats run the elite. Do not
      optimize the population metrics.
- [x] 7. `npm run train -- --generations --population --episodes --seed` writes
      `docs/weights-latest.json` and `docs/train-log.md`.

## Wiring

- [x] 8. `npm run simulate` accepts `--weights path` so a trained scorer can sit in the
      same 8-vs-56 harness as the handwritten baselines.
- [x] 9. Stage A tests still pass. Add: same weights + same seed ⇒ identical `MatchState`.

## Later (not now)

- [ ] 10. MLP scorer if linear plateaus below the `aggressive` bar.
- [ ] 11. Stage C — PPO on the same encoders.
- [ ] 12. Load weights into `/watch` (generation / fitness columns).
