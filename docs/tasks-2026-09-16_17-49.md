# Tasks — RL stage A: policy seam, tests, baselines (2026-09-16_17-49)

Stage A only. No learning code here.

## Engine seam

- [x] 1. Export `getLegalActions(player)` from `src/game/match.ts`. Enumerate **all**
      non-empty suit subsets, not a 50/50 sample. Vaccine is legal whenever held (never on self),
      not gated on `opponent.infected`. Shotgun and Zombie stay always legal when held.
- [x] 2. Define `Policy` (`src/rl/policy.ts`):
      `(ctx: { view, actions, random }) => TablePlay`.
- [x] 3. Thread an optional `choosePlay` through `playRound` / `resolveDuel`; default to uniform
      legal play so `/watch` stays random.
- [x] 4. `randomLegal` lives in `src/rl/baselines.ts` as a uniform pick over the enumerated set.
      `match.ts` keeps a tiny default chooser so the watch page does not import `src/rl`.

## Observation

- [x] 5. `PlayerView` + `encodeView()` (`src/rl/features.ts`): own hand per suit, own specials, own
      infected flag, opponent **hand size only**, round, living count. No opponent cards, no
      opponent faction, no living zombie share.
- [x] 6. `encodeAction()`: pile sum / size / top rank, suit and hand shares, regulars left, special
      one-hot.

## Tests (vitest)

- [x] 7. Add vitest + `npm test` + `make test`.
- [x] 8. Deal: 7 cards each, one shotgun per player, one zombie per group, vaccine count honoured.
- [x] 9. Duel resolution: sums, Ace = 1, tie steals nothing, winner takes one card from the loser's
      pile.
- [x] 10. Specials: zombie trumps and is kept, infection adds a copy, vaccine is offered vs a human
      and vs a zombie, cancels only a zombie **played this duel** and never on self, a miss is
      spent, shotgun kills zombies only and is consumed either way.
- [x] 11. End conditions: elimination on zero number cards, shotgun kill, 20-round verdict.
- [x] 12. Determinism: same seed + same policies ⇒ identical `MatchState`.

## Headless runner

- [x] 13. `src/rl/runEpisode.ts`: `runEpisode(seed, seating) => { clears, rounds, verdict }`, pure
      Node, no DOM.
- [x] 14. `npm run simulate -- --seed --episodes --policy` printing clear rate and mean survival.

## Baselines

- [x] 15. `conservative` — smallest legal pile, hold specials.
- [x] 16. `aggressive` — play zombie whenever held, otherwise largest pile.
- [ ] 17. `hoarder` — skipped. Number piles are never forced empty at decision time, so this
      duplicates `conservative`.
- [x] 18. Differential harness: 8 candidate seats vs 56 reference seats, report
      `candidate clear − reference clear` over N seeds with a confidence interval.
- [x] 19. Record the baseline table in [baselines-2026-09-16.md](./baselines-2026-09-16.md).

## Later stages (not now)

- [ ] 20. Stage B — ES trainer, `generation` / `fitness` wired into the `/watch` snapshot.
- [ ] 21. Stage C — PPO on the same encoders, compared against ES.
