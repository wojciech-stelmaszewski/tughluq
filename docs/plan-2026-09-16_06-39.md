# Plan — pip ranks only (2026-09-16_06-39)

Status: implemented.

## Lock

Operator: no J / Q / K in play. Ace counts as **1**.

## Change

- `RANKS` is A, 2–10 (40 number cards per deck).
- `rankValue('A') === 1`. Drop face-card branches.
- Pool size follows `SUITS × RANKS`, not 52.
- HUD proof thumbs show pip cards only.

## Out of scope

Ties, pairing UI, random shotgun policy.
