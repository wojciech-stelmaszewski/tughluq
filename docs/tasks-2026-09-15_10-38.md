# Tasks — Watch panel (2026-09-15_10-38)

- [x] 1. Add tournament constants (`GROUP_COUNT = 4`, `GROUP_SIZE = 16`, `TOURNAMENT_PLAYERS = 64`) without changing 3D `MAX_PLAYERS`.
- [x] 2. Add `dealTournament(params, seed)` that composes four `deal()` calls and unique player ids / names 01–64.
- [x] 3. Add `WatchSnapshot` + `snapshotFromTournament()` (generation, episode, round, derived faction stats).
- [x] 4. Switch `main.tsx` on `pathname`: `/` → existing App, `/watch` → WatchPage. SPA fallback in Vite.
- [x] 5. Build `WatchPage`: top bar (generation / episode / round / seed / factions), stat chips, 64-row table. No 3D.
- [x] 6. Add reciprocal nav links (`/` ↔ `/watch`). Scope `overflow: hidden` to the lab root so the watch page can scroll.
- [x] 7. Point README at `/watch`. Update `docs/plan.md` improvements log.
- [x] 8. Verify in the browser: 64 rows, 4 zombies (one per group), no WebGL on `/watch`, lab at `/` still deals 2–16.
- [x] 9. Show card-face graphics for every hand on `/watch` (row thumbs + selected inspect).
