# Tasks — MLP scorer (2026-09-17_19-19)

## Features

- [x] 1. `FEATURE_SCALE` (30 constants) + `normalizedFeatures(view, play)` in
      `src/rl/features.ts`. Leave `concatFeatures` alone so `linear-v1` files still reproduce.

## Model

- [x] 2. `Model` type (`dim`, `init(seed)`, `policy(params)`) in `src/rl/model.ts`;
      `LINEAR_MODEL` wraps the existing scorer.
- [x] 3. `src/rl/mlp.ts`: 30 → 8 `tanh` → 1, `mlpDim(hidden)`, seeded small random `init`,
      `mlpPolicy(params, hidden)`, `kind: 'mlp-v1'` with `hidden` in the file.
- [x] 4. `loadModelFile(raw)` dispatching on `kind`; `npm run simulate --weights` keeps
      reading `linear-v1`.

## Trainer

- [x] 5. `trainEs` takes a `Model` instead of calling `zeroWeights` / `scoredPolicy`.
- [x] 6. `--model linear|mlp` and `--hidden` in `src/rl/trainCli.ts`; both in the log header.

## Tests

- [x] 7. `mlpPolicy` is a `Policy`; same params + seed ⇒ identical `MatchState`.
- [x] 8. `loadModelFile` round-trips both kinds and rejects a bad `kind`.
- [x] 9. Committed `linear-v1` weights still score identically (guards the untouched
      `concatFeatures` path).
- [x] 9a. Not planned: every normalized feature stays inside [-1, 1].

## Runs

- [x] 10. Measure seconds per generation for the MLP: ~5.5 s at 16 × 30 with probe 60, about
      double after the first promotion. The MLP is barely slower than linear — enumerating
      legal actions dominates, not the arithmetic.
- [x] 11. Sigma sweep: only `sigma = 0.05` finished. Held out at 200 episodes it scores
      **+0.009** vs `randomLegal`, CI [−0.015, 0.034] — indistinguishable from random play
      after 20 generations. Sweep abandoned; see the finding below.
- [x] 12. Long run: done from the warm start instead, 40 × 24 × 100 at sigma 0.1.
- [x] 13. Held-out 300 episodes vs `randomLegal` (+0.247) and vs `aggressive` (+0.001).

## Finding: the experiment tested the wrong thing

The run answers “can a (1+λ) hill climber find a good 257-parameter network from scratch?”
(no) rather than “does extra capacity beat the linear plateau?”, which is the question that
justified the task. Two reasons, both about the search rather than the model:

1. **Bounded output at init.** The linear scorer drives the softmax by pushing one weight to
   about 8, which one or two `sigma = 0.2` steps achieve. The MLP output is a sum of eight
   `tanh` units scaled by output weights near ±0.18, so the whole score range starts around
   ±1.4 and the softmax is nearly flat. Getting to a decisive preference needs the output
   layer to grow by an order of magnitude, at 0.05 per generation.
2. **257 dimensions, 16 candidates.** Best-of-16 random directions is a weak search in that
   space. The linear model had 30 dimensions and 24 candidates.

Also: wall clock in the scratch log is misleading. Generations 1–14 took about 20 s each;
15–20 report thousands of seconds because the machine slept overnight.

## Next attempt (needs review)

- [x] 17. Warm-start the MLP from the trained linear weights. Verified before training: the
      warm-started network scores +0.267 vs `randomLegal` against the linear policy's +0.265.
- [x] 17a. Not planned: per-layer step scaling. The warm start puts input weights near 0.03
      beside output weights near 44, so one shared sigma moves the first layer by 150% and the
      second by 0.1%. Models without a `sigmaScale` keep one shared sigma, and the Stage B
      linear run still reproduces generation for generation.
- [x] 17b. 40 × 24 × 100 from the warm start: **+0.001** vs `aggressive`, CI [−0.019, 0.021].
      Eighty minutes of search moved nothing.
- [ ] 18. Mirrored sampling instead of elite selection. **Dropped for now** — with the warm
      start landing on the plateau and three sigmas behaving alike, the search is no longer
      the prime suspect.

## Conclusion: the model was not the constraint

Budget, starting point and step size have each been ruled out. The next suspect is the
observation: the policy cannot see the opponent's faction, so shotgun and vaccine are
lotteries rather than decisions. Test it cheaply by adding faction and zombie share to the
view and retraining the **linear** model — if that beats `aggressive`, information was the cap.

## Record

- [x] 14. `docs/train-log.md`: MLP section next to the linear one, honest reading.
- [x] 15. `docs/rl-explained.md`: the MLP path, why normalization and a non-zero init are
      required there but not for linear, and the closing verdict on the whole exercise.
- [x] 16. `docs/plan.md` improvements log; commit.
