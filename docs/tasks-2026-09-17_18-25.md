# Tasks — real training budget (2026-09-17_18-25)

No algorithm change. Budget and instrumentation only.

## Monitoring

- [x] 1. `TrainOptions.probeEpisodes`, independent of `episodes`; drop the
      `Math.min(options.episodes, 4)` cap in `src/rl/train.ts`.
- [x] 2. `--probe`, `--sigma`, `--promote` in `src/rl/trainCli.ts`; print all of them in the
      log header so a run is reproducible from its own log.
- [x] 2a. Not planned: `onGeneration` callback, so a 30-minute run reports per generation
      instead of printing everything at the end.

## Run

- [x] 3. Baseline re-run at the old budget for the record, into a scratch path (not
      `docs/train-log.md`). Reproduced the committed log exactly, which confirms the probe
      change does not touch the search.
- [x] 4. Full run: 60 generations × 24 population × 50 episodes, probe 150, seed 1.
      47 minutes; slower after the first promotion, because the reference seats then run the
      scored policy.
- [x] 5. Held-out check at 200 episodes: elite vs `randomLegal`, elite vs `aggressive`.

## Record

- [x] 6. Rewrite `docs/train-log.md` with the real budget and an honest reading of it.
- [x] 7. Correct the plateau claim in `docs/rl-explained.md` — the old −0.070 was noise; the
      elite ties `aggressive` at +0.003.
- [x] 8. `docs/plan.md` improvements log.
- [x] 9. Tests still pass; commit.

## Outcome

Outcome 2 from the plan, with a twist: the elite did not beat `aggressive`, but it no longer
loses to it either. Sixty generations moved the head-to-head from −0.433 to zero and then
stopped dead for forty generations. The linear model is the binding constraint, so task 10
(MLP scorer) is now justified rather than speculative.
